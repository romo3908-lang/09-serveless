{
  "name": "09-serverless-completo",
  "version": "1.0.0",
  "description": "Gestor de Tareas -- version completa (frontend + auth + tareas), 100% serverless (Vercel + Neon Postgres)",
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0"
  }
}
